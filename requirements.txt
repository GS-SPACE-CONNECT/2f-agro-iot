# 2F-AGRO · Olho na Folha — dependências (versões FIXAS, exigência da rubrica).
#
# Testado em macOS Intel (x86_64) + Python 3.9 e no Google Colab (Python 3.10+ / GPU).
# Nota técnica: torch 2.2.2 é a última versão com wheel oficial pra Intel Mac e foi
# compilado contra NumPy 1.x — por isso fixamos numpy<2 e um opencv compatível.
# Em GPU/Colab você pode soltar esses pins, mas estes valores reproduzem 1:1 o ambiente.

ultralytics==8.4.60
torch==2.2.2
torchvision==0.27.0
opencv-python==4.10.0.84
numpy==1.26.4
pillow==11.3.0
requests==2.32.5

# Fallback de webcam via WinRT (só Windows) — usado quando o backend clássico
# do OpenCV (MSMF/DSHOW) está quebrado na máquina. Ver src/camera_winrt.py.
winrt-runtime==3.2.1; sys_platform == "win32"
winrt-Windows.Foundation==3.2.1; sys_platform == "win32"
winrt-Windows.Foundation.Collections==3.2.1; sys_platform == "win32"
winrt-Windows.Devices.Enumeration==3.2.1; sys_platform == "win32"
winrt-Windows.Media==3.2.1; sys_platform == "win32"
winrt-Windows.Media.Capture==3.2.1; sys_platform == "win32"
winrt-Windows.Media.Capture.Frames==3.2.1; sys_platform == "win32"
winrt-Windows.Media.Devices==3.2.1; sys_platform == "win32"
winrt-Windows.Media.MediaProperties==3.2.1; sys_platform == "win32"
winrt-Windows.Graphics.Imaging==3.2.1; sys_platform == "win32"
winrt-Windows.Storage.Streams==3.2.1; sys_platform == "win32"
